VITE_API_URL=
VITE_TIME_ZONE=America/Manaus

# O proxy do Vite encaminha /api para o backend; deixe vazio para usar /api/v1 no mesmo host.
# HTTPS contínuo é controlado por AUTOPACKLINE_HTTPS no docker-compose/.env.
